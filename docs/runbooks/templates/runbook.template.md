# Runbook: REPLACE_ME (condition or task name)

> Copy this file, rename it (for example `high-latency.md`, `failover.md`),
> and replace every REPLACE_ME. Plain GitHub-flavored Markdown, not MDX.

- **Status:** REPLACE_ME (draft / reviewed / current)
- **Last reviewed:** REPLACE_ME (YYYY-MM-DD)
- **Owning team/role:** REPLACE_ME
- **Related ADRs:** REPLACE_ME (see [`../adr/`](../../adr/README.md))

## Purpose

REPLACE_ME: what operational condition or task this runbook addresses, and
the outcome it aims to restore or achieve.

## Triggers

REPLACE_ME: the alert(s), symptom(s), or request(s) that should lead
someone to this runbook. Be specific (alert name, threshold, dashboard
panel) so the right runbook is found quickly.

## Prerequisites

REPLACE_ME: access, tools, or permissions required before starting (for
example, a specific role, CLI, or break-glass access procedure).

## Safeguards

REPLACE_ME: guardrails to check before taking action (for example, confirm
blast radius, notify stakeholders, or check a maintenance window) and any
actions that must never be taken without additional approval.

## Diagnosis

REPLACE_ME: step-by-step checks to confirm the condition and gather the
information needed to choose a mitigation.

1. REPLACE_ME
2. REPLACE_ME

## Mitigation

REPLACE_ME: step-by-step mitigation actions, in order, including expected
results after each step.

1. REPLACE_ME
2. REPLACE_ME

## Verification

REPLACE_ME: how to confirm the mitigation worked (signals to check,
expected values/ranges).

## Rollback

REPLACE_ME: how to undo the mitigation if it did not help or made things
worse.

## Escalation

REPLACE_ME: who/what to escalate to, and under what condition (for example,
mitigation did not work within REPLACE_ME minutes, or the safeguard checks
above were not satisfiable).

## Evidence capture

REPLACE_ME: what to record after using this runbook (timestamps, actions
taken, outcome) and where — link to an
[evidence record](../../evidence/README.md) template if this event should
produce one.

## Related ADRs

REPLACE_ME: link the ADR(s) that explain why the system behaves this way or
why this mitigation is the accepted approach; see the
[ADR index](../../adr/README.md).
