# ADR-NNNN: REPLACE_ME (short decision title)

> Copy this file to `NNNN-short-decision-slug.md` using the next sequential
> ID (see [`../README.md`](../README.md#numbering-convention)), and replace
> every REPLACE_ME. This is plain GitHub-flavored Markdown, not MDX.

- **ID:** ADR-NNNN (stable once assigned; never reused)
- **Status:** proposed <!-- proposed | accepted | rejected | deprecated | superseded by ADR-XXXX -->
- **Date:** REPLACE_ME (YYYY-MM-DD)

## Context

REPLACE_ME: what problem or force requires a decision. Describe the
situation as it actually was, not as it is convenient to remember later.

## Decision drivers

REPLACE_ME: the requirements, constraints, and priorities that shaped this
decision (for example, cost ceiling, latency target, team skill set,
compliance requirement).

## Considered alternatives

REPLACE_ME: list credible alternatives that were actually considered, with
a short summary of why each was or was not chosen. An ADR with only one
option considered is a red flag; if there truly was only one viable option,
say so and explain why.

1. **Alternative A** — REPLACE_ME
2. **Alternative B** — REPLACE_ME

## Decision

REPLACE_ME: the decision, stated plainly and unambiguously.

## Consequences

REPLACE_ME: what becomes easier or harder as a result of this decision,
including trade-offs accepted knowingly.

### Operational implications

REPLACE_ME: effects on runbooks, on-call, monitoring, or day-2 operations.
Link to the affected [runbook(s)](../../runbooks/README.md).

### Security implications

REPLACE_ME: effects on identity, secrets, data classification, or attack
surface. If none, state that explicitly rather than omitting the section.

## Evidence

REPLACE_ME: link to any [evidence record(s)](../../evidence/README.md) that
informed this decision (benchmarks, spikes, incident data). If the decision
was made without evidence, say so rather than implying validation that
did not happen.

## Revisit triggers

REPLACE_ME: concrete conditions under which this decision should be
revisited (for example, a scale threshold, a deprecation notice from a
vendor, or a changed constraint from "Decision drivers").
